footprint: [[-0.21, -0.15], [-0.21, 0.15], [0.22, 0.15], [0.22, -0.15]]

transform_tolerance: 0.5

obstacle_layer:
  enabled: true
  obstacle_range: 2.0
  raytrace_range: 2.5
  max_obstacle_height: 2.0
  track_unknown_space: false
  observation_sources: scan
  scan:
    sensor_frame: laser_link           # 對應 URDF 中的 laser_link
    data_type: LaserScan
    topic: scan
    marking: true
    clearing: true

inflation_layer:
  enabled: true
  cost_scaling_factor: 10.0
  inflation_radius: 0.18